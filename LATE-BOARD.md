# Branches

- **main** — live production (GitHub Pages). Do not ship board-runner work here until Peter says go.
- **board-runner** — assignment sheet upload, On deck, shift letters (`*` early, `o/` orientation), closed/unlisted rooms. Preview only.

Live Pages stays at `1787844240320`. This branch does **not** bump Firebase `minVersion`.

Morning upload of weekday CRNA grid or weekend split `.xlsx`.
Names + shift letters land on rooms. CLOSED or missing rooms deactivate.
Late stay is a “can stay 2 hrs” flag, not a second listing.
Breakers sit on On deck. Late = M+S. Dinner = Q+W+E.
